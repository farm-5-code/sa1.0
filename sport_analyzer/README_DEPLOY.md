# 🚀 Деплой (самый простой — Streamlit Community Cloud)

## Рекомендуемая структура репозитория (как в этом проекте)
Оставляй файлы так, как они лежат сейчас:
- `requirements.txt` — в корне репозитория
- папка пакета `sport_analyzer/` — рядом

### Streamlit Community Cloud (бесплатно)
1) Создай репозиторий на GitHub.
2) Загрузи **весь проект целиком** (корень с `requirements.txt` + папка `sport_analyzer/`).
3) В Streamlit Community Cloud: **New app** → выбери репозиторий → укажи файл:
   **`sport_analyzer/dashboard/app.py`**
4) В настройках **Secrets** добавь ключи (по аналогии с `.env.example`):
   - `FOOTBALL_DATA_KEY`
   - `NEWS_API_KEY` (опционально)
   - `GNEWS_KEY` (опционально)

## Альтернативы
### Render (есть бесплатный тариф, но может «засыпать»)
- Web Service → Python → старт-команда:
  `streamlit run sport_analyzer/dashboard/app.py --server.port $PORT --server.address 0.0.0.0`

## Важно
- Реальный секретный `.env` **не коммить**. Для деплоя используй Secrets/Variables у платформы.
